# Менеджер паролей

Простой менеджер паролей на **Python** с графическим интерфейсом. Приложение позволяет безопасно хранить пароли и
связанные с ними данные, а также выполнять поиск по описанию.

---

## Функционал

* При запуске `main.py` открывается окно для создания или ввода **мастер-пароля**.

* После успешного входа доступны две основные кнопки:

    * **Создать** — добавить новую запись с паролем.
    * **Прочитать** — просмотреть сохранённые пароли.
    * **Импортировать** — можно импортировать пароли из Chrome
    * **Экспортировать** — можно экспортировать пароли, что-б потом импортировать их в новый Chrome

* Реализован поиск по описанию:

    * поиск нечувствителен к регистру;
    * поддерживаются частичные совпадения (например, по запросу `вкон` будет найдена запись `ВКонтакте`).

## 🗂 Шаг 1. Создаём папку для программы

Папка — это «домик», где будет жить программа.

### 🐧 Linux / 🍎 macOS

Откройте **Терминал** и вставьте:

```bash
mkdir -p ~/projects/pass_manager
cd ~/projects/pass_manager
```

### 🪟 Windows

Откройте **PowerShell** и вставьте:

```powershell
mkdir $HOME\projects\pass_manager
cd $HOME\projects\pass_manager
```

---

## 🧰 Шаг 2. Устанавливаем Python и Git (только для Windows)

### Python

```bash
winget install Python.Python.3.12
```

### Git

```bash
winget install Git.Git
```

**Важно:** после установки закройте PowerShell и откройте заново.

---

## 📦 Шаг 3. Скачиваем программу с интернета

```bash
cd $HOME\projects\pass_manager
git clone https://github.com/alex-sid-dev/pass_manager.git
```

Теперь программа находится в папке `pass_manager`.

---

## ⚙️ Шаг 4. Устанавливаем UV (помощник для зависимостей)

```bash
python -m pip install uv
```

---

## 🧩 Шаг 5. Устанавливаем все нужные зависимости

```bash
uv sync
```

После этого программа готова к запуску.

---

# 🚀 Как запускать программу

## 🪟 Windows — запуск через ярлык

1. На рабочем столе **создайте ярлык**.
2. В поле «Объект» вставьте:

```text
"C:\Путь_к_проекту\pass_manager\.venv\Scripts\pythonw.exe" "C:\Путь_к_проекту\pass_manager\main.py"
```

3. Нажмите **Готово**.

Теперь можно запускать программу двойным щелчком.

---

## 🐧 Linux — два способа

### Вариант 1: Через терминал

```bash
cd /путь/к/проекту/pass_manager
uv run python main.py
```

### Вариант 2: Ярлык в меню

1. Создайте файл ярлыка:

```bash
nano ~/.local/share/applications/pass_manager.desktop
```

2. Вставьте текст (замените пути):

```ini
[Desktop Entry]
Name = Password Manager
Comment = Простой менеджер паролей
Exec = /путь/к/проекту/pass_manager/.venv/bin/python /путь/к/проекту/pass_manager/main.py
Icon = dialog-password
Terminal = false
Type = Application
Categories = Utility;Security;
```

3. Сделайте файл исполняемым:

```bash
chmod +x ~/.local/share/applications/pass_manager.desktop
```

Программа появится в меню приложений.

---

## 🔐 Важно помнить

* Мастер-пароль защищает все сохранённые записи.
* Если мастер-пароль забыть — данные нельзя восстановить.
* Перед запуском убедитесь, что все зависимости установлены через **UV**.
